import { extractVideoUrl } from './src/lib/video-extractor.js';

// We need to compile or run via tsx, wait, astro dev runs ts, we can just use ts-node or run via node if we compile, but we have Astro.
// Let's just write a test script that runs via node with tsx? Wait, does the project have tsx?
